# Instance Configuration
INSTANCE_MASTER_KEY=generate_with_openssl_rand_hex_32
DOMAIN=buildops.example.com
BOOTSTRAP_ADMIN_TOKEN=generate_with_openssl_rand_hex_16

# JWT
JWT_SECRET=generate_with_openssl_rand_hex_32

# Database (for docker compose)
POSTGRES_USER=buildops
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=buildops

# MinIO (for docker compose)
MINIO_ROOT_USER=buildops
MINIO_ROOT_PASSWORD=change_me_in_production

# Optional: Override defaults
# NODE_ENV=production
# DATABASE_URL=postgresql://user:pass@host:5432/db
# REDIS_URL=redis://host:6379
